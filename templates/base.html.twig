<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Lieux insolites !!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header>
            <nav data-controller="search" data-details-url="{{ path('place_details', {'id': 0}) }}">
                <label for="search_box">rechercher</label>
                <input type="text" id="search_box" data-action="input->search#searching">
                <ul data-search-target="search_results">
                    {# {% for place in search_results %} #}
                        {# <a href="{{ path('place_details', {'id': place.id ?? 0}) }}"> #}
                            {# <li data-display-target="search_results"></li> #}
                        {# </a> #}
                    {# {% endfor %} #}
                </ul>
            </nav>
            
        </header>
        {% block body %}{% endblock %}
    </body>
</html>
