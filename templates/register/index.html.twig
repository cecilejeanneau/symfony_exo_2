{# {% extends 'base.html.twig' %}

{% block title %}Hello RegisterController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/cecil/Documents/projects/Symfony/exo_2/src/Controller/RegisterController.php'|file_link(0) }}">src/Controller/RegisterController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/cecil/Documents/projects/Symfony/exo_2/templates/register/index.html.twig'|file_link(0) }}">templates/register/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %} #}


{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

    <h1>Inscription</h1>

    {{ form_start(form) }}
        {{ form_row(form.email) }}
        {{ form_row(form.plaintextPassword, {
            label: 'Password'
        }) }}
        {{ form_row(form.username) }}

        <button type="submit" class="btn">S'inscrire</button>
    {{ form_end(form) }}

    {% if form.isSubmitted() and not form.isValid() %}
        <div class="errors">
            {% for error in form.errors %}
                <span>{{ error.message }}</span>
            {% endfor %}
        </div>
    {% endif %}
    
{% endblock %}
